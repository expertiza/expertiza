Hi <%= @first_name %>,

A new <%= @type %> for the "<%= @obj_name %>" assignment has just been entered.
You may log into Expertiza and view it now.

<% if @review_scores != nil %>
  Review Content:
  <% @review_scores.each { |@rs| %>
    -----------------------------------------------------------
    Question: <%= Question.find(@rs.question_id).txt %>

    Comment: <%= @rs.comments %>

    Score: <%= @rs.answer %>
  <% } %>
<% end %>

<% unless @ror_review_scores.nil? %>
  Review Content:
  <% @ror_review_scores.each { |@rs| %>
    -----------------------------------------------------------
    Question: <%= Question.find(@rs.question_id).txt %>

    Comment: <%= @rs.comments %>

    Score: <%= @rs.answer %>
  <% } %>
<% end %>
