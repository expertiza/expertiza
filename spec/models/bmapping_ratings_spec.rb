require 'spec_helper'

describe BmappingRatings do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    BmappingRatings.create!(@valid_attributes)
  end
end
