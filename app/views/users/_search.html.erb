<!-- Javascript code to toggle on and off the Advanced Search button text -->
<script>
    $(document).ready(() => {
        const $toggleButton = $('#search_toggle_button');
        const $advancedSearchForm = $('#advance_search_form');
        let isAdvancedSearchVisible = false;
        $advancedSearchForm.hide();
        $toggleButton.click(() => {
            isAdvancedSearchVisible = !isAdvancedSearchVisible;
            toggleAdvancedSearch(isAdvancedSearchVisible);
        });
        function toggleAdvancedSearch(isAdvancedSearchVisible) {
            if (isAdvancedSearchVisible) {
                $advancedSearchForm.show();
            } else {
                $advancedSearchForm.hide();
            }
        }
    });
</script>

<!-- Username TextBox with Search button and a Advanced Search with text based on its' state: toggled on or off -->

<div style="margin: 8px auto; display: grid; grid-template-columns: repeat(3, auto) 1fr; grid-gap: 8px; align-items: center">
  <%= label_tag :search_usrid, 'UserID: ' %><%= text_field_tag(:search_usrid,params[:search_usrid], class:'form-control', placeholder: 'Search UserID...') %>
  <button type="submit" class="btn btn-primary">Search</button>
  <label><input type="checkbox" id="search_toggle_button"/>Advanced Search</label>
</div>

<!-- Name and Email TextBoxes to be displayed on tap of Advanced Search button -->

<div id="advance_search_form" style="margin: 8px auto; display: grid; grid-template-columns: repeat(4, auto) 1fr; grid-gap: 8px; align-items: center">
  <%= label_tag :search_full_name, 'Name: ' %><%= text_field_tag(:search_fname,params[:search_fname], class:'form-control', placeholder: 'Search Name...') %>
  <%= label_tag :search_email, 'Email: ' %><%= text_field_tag(:search_email,params[:search_email], class:'form-control', placeholder: 'Search E-mail...') %>
</div>