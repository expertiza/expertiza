<h1>Edit Bookmark</h1>

<% form_tag :action => 'update'do %>


  <%= render :partial => 'topic' %>
  <%= hidden_field_tag('id', params[:id]) %>
  <%= hidden_field_tag('assignment_id', params[:assignment_id]) %>
  <%= submit_tag "Update"%>
<% end %>

<%= link_to 'Back', :action => 'index', :id => params[:assignment_id]%>
