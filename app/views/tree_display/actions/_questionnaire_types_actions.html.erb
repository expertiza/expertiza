<TD WIDTH="15%">
  <ul id="actionmenu">
    <li class="daddy">&nbsp;
      <ul>        
        <% model = TreeFolder.find(node.node_object_id).name+"Questionnaire"
           model.gsub!(/[^\w]/,'') %>
        <li><%= link_to 'Create Public '+node.get_name, :controller=>'questionnaire', :action=>'new', :model => model, :private => 0 %></li>  
        <li><%= link_to 'Create Private '+node.get_name, :controller=>'questionnaire', :action=>'new', :model => model, :private => 1 %></li>          
      </ul>
    </li>
  </ul>        
</TD>