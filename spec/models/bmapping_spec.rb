require 'spec_helper'

describe Bmapping do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Bmapping.create!(@valid_attributes)
  end
end
