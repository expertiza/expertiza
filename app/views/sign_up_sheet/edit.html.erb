<h1>Edit topic</h1>

<%= start_form_tag :action => 'update'%>
  

  <%= render :partial => 'topic' %>
  <%= hidden_field_tag('id', params[:id]) %>
  <%= hidden_field_tag('assignment_id', params[:assignment_id]) %>
  <%= submit_tag "Update"%>
<%= end_form_tag %>

<%= link_to 'Back', :action => 'add_signup_topics', :id => params[:assignment_id]%>