require 'rails_helper'

describe "scale" do
  before(:each) do
    @scale = build(:scale)
  end
  # it "scale is valid" do
  #   expect(@scale).to be_valid
  # end
  it "returns a html if an instructor is creating or editing a questionnaire "
  it "returns what to display if an instructor is viewing a questionnaire"
  it "return what to display when students do peer reviews"
  it "return what to diaplay when student view existed peer reviews"

end