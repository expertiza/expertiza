Updated Assignment:

<% if activity.trackable %>
    <%= link_to activity.trackable.name, activity.trackable %>
    in course <%= link_to Course.find(activity.trackable.course_id).name %>
<% else %>
    which has since been removed
<% end %>