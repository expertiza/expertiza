<%= render :partial => 'submitted_content/title' %>    
<% topic_id = SignedUpTeam.topic_id(@participant.parent_id, @participant.user_id) %>
<%@stage = @assignment.get_current_stage(topic_id)%>
<%= render :partial => 'submitted_content/main', :locals => {:participant => @participant, :stage => @stage, :can_submit =>@can_submit} %>
<!-- E726 Changes Begin -->
<%= render :partial => 'submitted_content/selfreview'%>
<!-- E726 Changes End -->
<!-- E1578 Timestamps Begin -->
<b>List of Events</b>
<%= will_paginate @events %>
<%= render :partial => 'submitted_content/events_list', :locals => {:participant_id => @participant.id, :team_id => @team_id} %>
<%= will_paginate @events %>
<!-- E1578 Timestamps End -->
<BR/>
<BR/>
<a href="javascript:window.history.back()">Back</a>