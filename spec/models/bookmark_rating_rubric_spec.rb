require 'spec_helper'

describe BookmarkRatingRubric do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    BookmarkRatingRubric.create!(@valid_attributes)
  end
end
