%h1 Assignments
= render :partial => 'shared_scripts/letters'
%table
  %tr
    %th Assignment Name
    %th Assignment Directory
  - for assignment in @assignments
    %tr
      %td= link_to assignment.name, :action => 'list_mappings', :id => assignment.id
      %td
        = assignment.directory_path
= render :partial => 'shared_scripts/pages'
= render :partial => 'shared_scripts/letters'