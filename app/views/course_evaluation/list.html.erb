<h1> Pending Course Evaluations </h1>

<table class=listing cellpadding=2 width=50%>
<% if (@surveys.size > 0) %>
  <tr><th align = left> Course </th>
    <th align=left> Due Date </th></tr>
  <% i=0 %>
  <% @surveys.each do |s| %>
    
    <tr><td align = left><%= link_to s[0].name ,:controller=>'course_evaluation',:action=>'create_response_map',:assignment_id => s[1] %>&nbsp &nbsp</td>
    </tr>
  
  <% end %>
<% else %>
  <tr>
    <td>
      <div class="flash_note">
        There exist no pending course evaluations.
      </div>
  </td> </tr>
<%end%>

</table>
