%h1 Listing evidences

%table
  %thead
    %tr
      %th Awarded badge
      %th File name
      %th
      %th
      %th

  %tbody
    - @evidences.each do |evidence|
      %tr
        %td= evidence.awarded_badge_id
        %td= evidence.file_name
        %td= link_to 'Show', evidence
        %td= link_to 'Edit', edit_evidence_path(evidence)
        %td= link_to 'Destroy', evidence, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Evidence', new_evidence_path
