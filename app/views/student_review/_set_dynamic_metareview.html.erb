<%= form_tag :controller => 'review_mapping',
  :action => 'assign_metareviewer_dynamically',
  :assignment_id => assignment.id,
  :metareviewer_id => session[:user].id do %>
  <input type='submit' value='Request a new meta-review to perform'/>
<% end %>
