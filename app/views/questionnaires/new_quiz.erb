<%= render :partial => 'new_quiz_script' %>
<h1>New <%= @itemnaire.display_type %></h1>
<% if @itemnaire.type == "QuizQuestionnaire" %>    <!--handle a itemnaire differently if it is a quiz-->
  <%= form_tag :controller => 'quiz_itemnaires', :action => 'create',
               :pid => @participant_id, :aid => @assignment_id do %>
    <% if @itemnaire.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@itemnaire.errors.count, "error") %> prohibited this article from being saved:</h2>
        <ul>
          <% @itemnaire.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <% $disp_flag = 1 %>
    <%= render :partial => 'quiz_itemnaire' %>

    <% $disp_flag = 0 %>

    <%= render :partial => 'new_item_template' %>

    <br>
    <%= submit_tag "Create Quiz", :name => "save" %>
  <% end %>
<% else %>

<% end %>
<br/>
<a href="javascript:window.history.back()">Back</a>
